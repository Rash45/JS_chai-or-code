/* how to keep  data into memory and use their access 
   on ttypehis we are categarized into two types 
   1. premitive datatype
   2. non-premitive data

   # premitive
   7- types :  String, Number, Boolean, null, undefined, Symbol, BigInt

   # reference type (Non-premitive)  :  Array, Objects, Functions
*/

const score = 100 // Number
const scoreValue = 100.3  //Number

const isLoggedIn = false //boolean
const outsideTemp = null // Null
let userEmail; // undefined

const id = Symbol('123')
const sumId = Symbol('123')
 //console.log(id===sumId);
  
 const bigNumber = 86684684646487987n

 const heros = ["shaktiman", "naagraj", "doga"];
 let myobj={
   name: "rash",
   age: 22,
 }
 //Anything inside the curlybraces  that value is object part

 const myFunction = function(){
   console.log("hello world");
   
 }

 console.log(typeof scoreVaue);
 

  
